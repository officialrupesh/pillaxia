import styled from "styled-components";

const HeaderStyled = styled.header`
  color: #000000;
  padding-block: 24px;
  margin-bottom: 50px;
  .header-right{
    gap: 24px;
  }
  ul {
      gap: 40px;
    li {
      font-size: 1.6rem;
      a{
        color: #000000;

      }
    }
  }
 
`;

export default HeaderStyled;